@extends('layouts.app')
@section('content')<section class="auth-page"><form class="auth-card" method="POST" action="{{ route('login.submit') }}">@csrf<span class="eyebrow">WELCOME BACK</span><h1>Sign in to Carolina.</h1><p>Manage bookings and reserve your next stay.</p><label>Email address<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><label>Password<span class="password-field"><input type="password" name="password" required><button class="password-toggle" type="button" aria-label="Show password" aria-pressed="false">Show</button></span></label><button class="button">Sign in</button><p class="auth-switch"><a href="{{ route('password.request') }}">Forgot password?</a></p><p class="auth-switch">New here? <a href="{{ route('register') }}">Create an account</a></p></form></section>@endsection
