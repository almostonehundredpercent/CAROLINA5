@extends('layouts.app')
@section('content')<section class="room-detail"><img src="{{ $room->image_url }}" alt="{{ $room->name }}"><div><span class="eyebrow">{{ $room->room_type }}</span><h1>{{ $room->name }}</h1><p class="lead">{{ $room->description }}</p><div class="facts"><span>🛏 {{ $room->beds }} bed{{ $room->beds > 1 ? 's' : '' }}</span><span>👥 Up to {{ $room->guests }} guests</span><span>⌁ Free Wi-Fi</span></div><h2>₱{{ number_format($room->price_per_night) }} <small>/ night</small></h2><div class="amenities">@foreach($room->amenities ?? [] as $amenity)<span>✓ {{ $amenity }}</span>@endforeach</div>@auth<a class="button" href="{{ route('bookings.create', $room) }}">Reserve this room</a>@else<a class="button" href="{{ route('login') }}">Sign in to reserve</a>@endauth</div></section>@endsection
