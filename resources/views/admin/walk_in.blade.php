<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Walk-in booking - Carolina Admin</title><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/admin.css') }}"><style>.walkin-card{max-width:760px}.walkin-card form{display:grid;grid-template-columns:1fr 1fr;gap:16px}.walkin-card label{display:grid;gap:7px;font-size:12px;font-weight:700;color:var(--muted)}.walkin-card input,.walkin-card select{border:1px solid var(--line);border-radius:7px;padding:11px;background:#fff;font:inherit;color:var(--ink)}.walkin-card .full{grid-column:1/-1}.walkin-submit{border:0;border-radius:7px;background:var(--orange);color:#fff;padding:13px 18px;font:700 14px 'DM Sans';cursor:pointer}.walkin-note{color:var(--muted);font-size:12px}@media(max-width:700px){.walkin-card form{grid-template-columns:1fr}}</style></head>
<body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}"><i>▦</i> Dashboard</a><a href="{{ route('admin.rooms') }}"><i>⌂</i> Rooms</a><a href="{{ route('admin.bookings') }}"><i>▤</i> Bookings</a><a class="active" href="{{ route('admin.walk-ins.create') }}"><i>＋</i> Walk-ins</a><a href="{{ route('admin.reports') }}"><i>⌁</i> Reports</a></nav><a href="{{ route('home') }}" style="position:absolute;bottom:84px;left:16px;right:16px;border:1px solid #fff;border-radius:8px;padding:12px;color:#fff;text-decoration:none">⌂ Go to home page</a><form method="POST" action="{{ route('logout') }}" class="admin-logout">@csrf<button class="logout-button" type="submit"><i>⎋</i> Log out</button></form></aside><main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">FRONT DESK</p><h1>New walk-in</h1><p class="admin-subtitle">Confirm a room and create an occupied booking immediately.</p></div></header>@if($errors->any())<div class="admin-flash" style="background:#fbe3e0;color:#a84336">{{ $errors->first() }}</div>@endif<section class="panel walkin-card"><form method="POST" action="{{ route('admin.walk-ins.store') }}">@csrf<label class="full">Available room<select name="room_id" required><option value="">Choose a room</option>@foreach($rooms as $room)<option value="{{ $room->id }}" @selected(old('room_id') == $room->id)>{{ $room->name }} — ₱{{ number_format($room->price_per_night) }}/night</option>@endforeach</select></label><label>Guest full name<input name="guest_name" value="{{ old('guest_name') }}" required></label><label>Phone number<input name="guest_phone" value="{{ old('guest_phone') }}" required></label><label>Email (optional)<input type="email" name="guest_email" value="{{ old('guest_email') }}"></label><label>Number of guests<input type="number" name="guests" min="1" value="{{ old('guests', 1) }}" required></label><label>Check in<input type="date" name="check_in" min="{{ now()->toDateString() }}" value="{{ old('check_in', now()->toDateString()) }}" required></label><label>Check out<input type="date" name="check_out" min="{{ now()->addDay()->toDateString() }}" value="{{ old('check_out', now()->addDay()->toDateString()) }}" required></label><label class="full">Payment method<select name="payment_method"><option value="gcash">GCash</option><option value="cash">Pay at property</option></select></label><p class="walkin-note full">This creates a confirmed booking immediately and marks the room occupied for the selected dates.</p><button class="walkin-submit full">Confirm walk-in booking</button></form></section></main></div></body></html>
