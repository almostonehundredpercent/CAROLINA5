<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $room->exists ? 'Edit room' : 'Add room' }} · Carolina</title><link rel="stylesheet" href="{{ asset('css/admin.css') }}"><style>.room-form{max-width:850px;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:15px}.room-form label{display:grid;gap:6px;font-size:12px;font-weight:700;color:var(--muted)}.room-form input,.room-form textarea,.room-form select{font:inherit;color:var(--ink);background:#fff;border:1px solid var(--line);border-radius:7px;padding:10px}.room-form textarea,.room-form .full{grid-column:1/-1}.room-form textarea{min-height:130px;resize:vertical}.room-form button{border:0;border-radius:7px;background:var(--orange);color:#fff;padding:12px;font-weight:700;cursor:pointer}.room-form .check{display:flex;align-items:center;gap:8px;color:var(--ink)}@media(max-width:650px){.room-form{grid-template-columns:1fr}}</style></head>
<body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}"><i>▦</i>Dashboard</a><a class="active" href="{{ route('admin.rooms') }}"><i>⌂</i>Rooms</a><a href="{{ route('admin.bookings') }}"><i>▤</i>Bookings</a><a href="{{ route('admin.walk-ins.create') }}"><i>+</i>Walk-ins</a><a href="{{ route('admin.reports') }}"><i>⌁</i>Reports</a><a href="{{ route('admin.activity') }}"><i>◷</i>Activity log</a>@if(auth()->user()->isAdmin())<a href="{{ route('admin.staff') }}"><i>♙</i>Staff access</a>@endif</nav></aside><main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">ROOM CATALOG</p><h1>{{ $room->exists ? 'Edit room' : 'Add room' }}</h1><p class="admin-subtitle">Only publish information verified by Carolina management.</p></div></header>@if($errors->any())<div class="admin-flash" style="background:#fbe3e0;color:#a84336">{{ $errors->first() }}</div>@endif
<section class="panel"><form class="room-form" method="POST" action="{{ $action }}" enctype="multipart/form-data">@csrf @if($method === 'PATCH')@method('PATCH')@endif
<label>Room name<input name="name" required value="{{ old('name', $room->name) }}"></label><label>URL slug<input name="slug" required pattern="[a-z0-9-]+" value="{{ old('slug', $room->slug) }}" aria-describedby="slug-help"></label><small id="slug-help">Lowercase letters, numbers, and hyphens only.</small>
<label>Room type<input name="room_type" required value="{{ old('room_type', $room->room_type) }}"></label><label>Maximum guests<input type="number" name="guests" min="1" max="30" required value="{{ old('guests', $room->guests ?: 1) }}"></label><label>Beds<input type="number" name="beds" min="1" max="20" required value="{{ old('beds', $room->beds ?: 1) }}"></label><label>Base price (₱)<input type="number" name="price_per_night" min="0" step="0.01" required value="{{ old('price_per_night', $room->price_per_night) }}"></label><label>Short-stay duration (optional)<select name="rental_hours"><option value="">Nightly stay</option>@foreach([3,6,12,22,24] as $hours)<option value="{{ $hours }}" @selected((int) old('rental_hours', $room->rental_hours) === $hours)>{{ $hours }} hours</option>@endforeach</select></label><label>Default check-in time<input type="time" name="default_check_in_time" value="{{ old('default_check_in_time', $room->default_check_in_time ? substr($room->default_check_in_time, 0, 5) : '') }}"></label><label>Image URL (optional)<input type="url" name="image_url" value="{{ old('image_url', $room->image_url) }}"></label><label>Upload image (JPG, PNG, WebP; 5 MB max)<input type="file" name="image" accept="image/jpeg,image/png,image/webp"></label><label>Amenities, separated by commas<input name="amenities" value="{{ old('amenities', implode(', ', $room->amenities ?? [])) }}"></label><label class="full">Description<textarea name="description" required>{{ old('description', $room->description) }}</textarea></label><label class="check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $room->exists ? $room->is_active : true))> Visible and bookable</label>@if($room->image_url)<label class="check"><input type="checkbox" name="remove_image" value="1"> Remove current image</label>@endif
<button type="submit">{{ $room->exists ? 'Save room changes' : 'Add room' }}</button><a href="{{ route('admin.rooms') }}">Cancel</a></form></section></main></div></body></html>
