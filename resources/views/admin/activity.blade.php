<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Activity log · Carolina</title><link rel="stylesheet" href="{{ asset('css/admin.css') }}?v={{ filemtime(public_path('css/admin.css')) }}"></head><body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}"><i>▦</i>Dashboard</a><a href="{{ route('admin.rooms') }}"><i>⌂</i>Rooms</a><a href="{{ route('admin.bookings') }}"><i>▤</i>Bookings</a><a href="{{ route('admin.walk-ins.create') }}"><i>+</i>Walk-ins</a><a href="{{ route('admin.reports') }}"><i>⌁</i>Reports</a><a class="active" href="{{ route('admin.activity') }}"><i>◷</i>Activity log</a>@if(auth()->user()->isAdmin())<a href="{{ route('admin.staff') }}"><i>♙</i>Staff access</a>@endif</nav><form method="POST" action="{{ route('logout') }}" class="admin-logout">@csrf<button class="logout-button">Log out</button></form></aside><main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">AUDIT TRAIL</p><h1>System activity</h1><p class="admin-subtitle">Important operational changes are recorded with the responsible account and time.</p></div></header><section class="panel"><div class="admin-table-wrap"><table><thead><tr><th>When</th><th>Action</th><th>Record</th><th>Performed by</th><th>Details</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at->format('M j, Y g:i A') }}</td><td><b>{{ str_replace('_', ' ', ucfirst($log->event)) }}</b></td><td>{{ $log->booking?->reference ?? class_basename($log->subject_type ?? 'System') . ($log->subject_id ? ' #' . $log->subject_id : '') }}</td><td>{{ $log->user?->name ?? 'Guest / system' }}</td><td>{{ $log->description }}</td></tr>@empty<tr><td colspan="5" class="no-bookings">No operational activity has been recorded yet.</td></tr>@endforelse</tbody></table></div>{{ $logs->links() }}</section></main></div></body></html>
