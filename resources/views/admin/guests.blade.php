<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Guests · Carolina</title><link rel="stylesheet" href="{{ asset('css/admin.css') }}?v={{ filemtime(public_path('css/admin.css')) }}"><style>.guest-filter{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px}.guest-filter input{min-width:min(100%,280px);padding:10px;border:1px solid var(--line);border-radius:7px;font:inherit}.guest-filter button{border:0;border-radius:7px;background:var(--orange);color:#fff;padding:10px 14px;font-weight:700;cursor:pointer}</style></head><body><div class="admin-shell">@include('admin.partials.sidebar')<main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">GUEST MANAGEMENT</p><h1>Guests</h1><p class="admin-subtitle">Search real booking records by guest name, email, or phone.</p></div></header>@include('admin.partials.flash')<section class="panel"><form class="guest-filter" method="GET" role="search"><label class="sr-only" for="guest-search">Search guests</label><input id="guest-search" name="search" value="{{ request('search') }}" placeholder="Name, email, or phone"><button type="submit">Search</button>@if(request('search'))<a href="{{ route('admin.guests') }}">Clear</a>@endif</form><div class="admin-table-wrap"><table><thead><tr><th>Guest</th><th>Phone</th><th>Bookings</th><th>Most recent stay</th><th></th></tr></thead><tbody>@forelse($guests as $guest)<tr><td><b>{{ $guest->name }}</b><small>{{ $guest->email ?: 'No email' }}</small></td><td>{{ $guest->phone ?: 'No phone' }}</td><td>{{ $guest->bookings }}</td><td>{{ $guest->latest->check_in->format('M j, Y') }} · {{ ucfirst($guest->latest->status) }}</td><td><a class="status-select" href="{{ route('admin.guests.show', $guest->token) }}">Open profile</a></td></tr>@empty<tr><td colspan="5" class="no-bookings">No guests match this search.</td></tr>@endforelse</tbody></table></div>{{ $guests->links() }}</section></main></div></body></html>
