<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Rooms - Carolina Admin</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/admin.css') }}"></head>
<body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}"><i>▦</i> Dashboard</a><a class="active" href="{{ route('admin.rooms') }}"><i>⌂</i> Rooms</a><a href="{{ route('admin.bookings') }}"><i>▤</i> Bookings</a><a href="{{ route('admin.reports') }}"><i>⌁</i> Reports</a></nav><a href="{{ route('home') }}" class="admin-home" style="position:absolute;bottom:84px;left:16px;right:16px;border:1px solid rgba(255,255,255,.85);border-radius:8px;padding:12px 13px;color:#fff;text-decoration:none;font:600 14px 'DM Sans',sans-serif;display:flex;gap:12px;align-items:center;"><i>⌂</i> Go to home page</a><form method="POST" action="{{ route('logout') }}" class="admin-logout">@csrf<button class="logout-button" type="submit"><i>⎋</i> Log out</button></form></aside>
<main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">PROPERTY MANAGEMENT</p><h1>Rooms</h1><p class="admin-subtitle">Live room availability and guest stay details.</p></div><div class="admin-user"><span class="avatar">{{ strtoupper(substr(auth()->user()->name, 0, 1)) }}</span><div><b>{{ auth()->user()->name }}</b><small>Administrator</small></div></div></header>@if(session('success'))<div class="admin-flash">{{ session('success') }}</div>@endif
<div class="room-legend"><span class="available">Available</span><span class="reserved">Reserved</span><span class="occupied">Occupied</span><span class="cleaning">Cleaning</span><span class="maintenance">Maintenance</span></div><section class="panel rooms-panel"><div class="admin-table-wrap"><table class="rooms-table"><thead><tr><th>Room</th><th>Type</th><th>Price / night</th><th>Status</th><th>Guest</th><th>Check-in</th><th>Check-out</th><th>Operations</th></tr></thead><tbody>@forelse($rooms as $room)<tr class="room-row {{ $room->display_status }}"><td><b>{{ $room->name }}</b><small>Up to {{ $room->guests }} guests · {{ $room->beds }} bed{{ $room->beds > 1 ? 's' : '' }}</small></td><td>{{ $room->room_type }}</td><td>₱{{ number_format($room->price_per_night) }}</td><td><span class="room-status {{ $room->display_status }}">{{ ucfirst($room->display_status) }}</span></td><td>{{ $room->display_booking?->guest_name ?? $room->display_booking?->user?->name ?? '—' }}</td><td>{{ $room->display_booking ? $room->display_booking->check_in->format('M j, Y') : '—' }}</td><td>{{ $room->display_booking ? $room->display_booking->check_out->format('M j, Y') : '—' }}</td><td><form method="POST" action="{{ route('admin.rooms.status', $room) }}">@csrf @method('PATCH')<select name="operational_status" onchange="this.form.submit()"><option value="available" @selected($room->operational_status === 'available')>Available</option><option value="cleaning" @selected($room->operational_status === 'cleaning')>Cleaning</option><option value="maintenance" @selected($room->operational_status === 'maintenance')>Maintenance</option></select></form></td></tr>@empty<tr><td colspan="8" class="no-bookings">No rooms have been added yet.</td></tr>@endforelse</tbody></table></div></section></main></div></body></html>
