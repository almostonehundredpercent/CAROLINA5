<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Reports - Carolina Admin</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/admin.css') }}"><style>.report-line{max-width:720px;margin-inline:auto}.report-line svg{height:auto;aspect-ratio:600/220}.report-line .chart-labels{max-width:720px;margin-inline:auto}.booking-filters{display:flex;flex-wrap:wrap;gap:8px;align-items:end;margin:0 0 18px}.booking-filters label{display:grid;gap:4px;font-size:11px;font-weight:700;color:var(--muted)}.booking-filters input{border:1px solid var(--line);border-radius:6px;padding:8px;background:#fff;font:inherit}.booking-filters button{border:0;border-radius:6px;padding:9px 12px;background:var(--orange);color:#fff;font-weight:700;cursor:pointer}.booking-filters small{color:var(--muted);font-size:11px}</style></head>
<body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}"><i>▦</i> Dashboard</a><a href="{{ route('admin.rooms') }}"><i>⌂</i> Rooms</a><a href="{{ route('admin.bookings') }}"><i>▤</i> Bookings</a><a href="{{ route('admin.walk-ins.create') }}"><i>＋</i> Walk-ins</a><a class="active" href="{{ route('admin.reports') }}"><i>⌁</i> Reports</a></nav><a href="{{ route('home') }}" class="admin-home" style="position:absolute;bottom:84px;left:16px;right:16px;border:1px solid rgba(255,255,255,.85);border-radius:8px;padding:12px 13px;color:#fff;text-decoration:none;font:600 14px 'DM Sans',sans-serif;display:flex;gap:12px;align-items:center;"><i>⌂</i> Go to home page</a><form method="POST" action="{{ route('logout') }}" class="admin-logout">@csrf<button class="logout-button" type="submit"><i>⎋</i> Log out</button></form></aside>
<main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">ANALYTICS</p><h1>Reports</h1><p class="admin-subtitle">Track earnings, bookings, and guest activity.</p></div><div class="admin-user"><span class="avatar">{{ strtoupper(substr(auth()->user()->name, 0, 1)) }}</span><div><b>{{ auth()->user()->name }}</b><small>Administrator</small></div></div></header>
<section class="report-tabs"><a class="{{ $metric === 'earnings' ? 'active' : '' }}" href="{{ route('admin.reports', ['metric' => 'earnings', 'period' => $period] + $range) }}">Earnings</a><a class="{{ $metric === 'bookings' ? 'active' : '' }}" href="{{ route('admin.reports', ['metric' => 'bookings', 'period' => $period] + $range) }}">Bookings</a><a class="{{ $metric === 'guests' ? 'active' : '' }}" href="{{ route('admin.reports', ['metric' => 'guests', 'period' => $period] + $range) }}">Guests</a><form method="GET" class="report-period"><input type="hidden" name="metric" value="{{ $metric }}"><button name="period" value="daily" class="{{ $period === 'daily' ? 'active' : '' }}">D</button><button name="period" value="weekly" class="{{ $period === 'weekly' ? 'active' : '' }}">W</button><button name="period" value="monthly" class="{{ $period === 'monthly' ? 'active' : '' }}">M</button><button name="period" value="yearly" class="{{ $period === 'yearly' ? 'active' : '' }}">Y</button></form></section>
<form method="GET" class="booking-filters" aria-label="Custom report range"><input type="hidden" name="metric" value="{{ $metric }}"><input type="hidden" name="period" value="custom"><label>From <input type="date" name="from" required value="{{ $range['from'] ?? '' }}"></label><label>To <input type="date" name="to" required value="{{ $range['to'] ?? '' }}"></label><button type="submit">Custom range</button><small>Up to 93 days.</small></form>
@if($metric === 'bookings')<section class="report-booking-metrics"><article><small>Total bookings</small><strong>{{ $bookingReportMetrics['total'] }}</strong><em>All reservation records</em></article><article><small>Cancelled bookings</small><strong>{{ $bookingReportMetrics['cancelled'] }}</strong><em>Reservations no longer active</em></article><article><small>Occupancy rate</small><strong>{{ $bookingReportMetrics['occupancy'] }}%</strong><em>Rooms occupied today</em></article><article><small>Completed bookings</small><strong>{{ $bookingReportMetrics['completed'] }}</strong><em>Confirmed completed stays</em></article></section>@endif
@if($metric === 'guests')<section class="report-booking-metrics guest-report-metrics"><article><small>Total guests</small><strong>{{ $guestReportMetrics['total'] }}</strong><em>Unique guests on record</em></article><article><small>New guests</small><strong>{{ $guestReportMetrics['new'] }}</strong><em>First booking this period</em></article><article><small>Returning guests</small><strong>{{ $guestReportMetrics['returning'] }}</strong><em>More than one booking</em></article><article><small>Average stay duration</small><strong>{{ $guestReportMetrics['averageStay'] }} nights</strong><em>Across all bookings</em></article></section>@endif
@php($maxValue = max(1, $chartValues->max()))<section class="panel report-main-card"><p class="admin-kicker">{{ strtoupper($period) }} {{ strtoupper($chartTitle) }}</p><strong class="report-total">{{ $metric === 'earnings' ? '₱' . number_format($total) : number_format($total) }}</strong><div class="line-graph report-line"><svg viewBox="0 0 600 220" preserveAspectRatio="none"><line x1="0" y1="200" x2="600" y2="200" class="graph-axis"/><line x1="0" y1="100" x2="600" y2="100" class="graph-grid"/><line x1="0" y1="35" x2="600" y2="35" class="graph-grid"/><polyline class="booking-line" points="@foreach($chartValues as $index => $value){{ $chartValues->count() > 1 ? ($index / ($chartValues->count() - 1) * 600) : 300 }},{{ 200 - ($value / $maxValue * 160) }} @endforeach"/>@foreach($chartValues as $index => $value)<circle cx="{{ $chartValues->count() > 1 ? ($index / ($chartValues->count() - 1) * 600) : 300 }}" cy="{{ 200 - ($value / $maxValue * 160) }}" r="5" class="booking-dot"><title>{{ $chartLabels[$index] }}: {{ $value }}</title></circle>@endforeach</svg><div class="chart-labels">@foreach($chartLabels as $label)<small>{{ $label }}</small>@endforeach</div></div></section>
<section class="report-bottom-grid"><article class="panel"><div class="panel-heading"><div><p class="admin-kicker">BREAKDOWN</p><h2>{{ $chartTitle }} by period</h2></div></div><div class="report-bars">@foreach($chartValues as $index => $value)<div><span style="height:{{ max(8, $value / $maxValue * 130) }}px"></span><b>{{ $metric === 'earnings' ? '₱' . number_format($value) : $value }}</b><small>{{ $chartLabels[$index] }}</small></div>@endforeach</div></article><article class="panel"><div class="panel-heading"><div><p class="admin-kicker">BOOKING DETAIL</p><h2>Latest bookings</h2></div></div><div class="report-table-wrap"><table class="report-table"><thead><tr><th>Date</th><th>Guest</th><th>Room</th><th>Total</th></tr></thead><tbody>@forelse($latestBookings as $booking)<tr><td>{{ $booking->created_at->format('Y-m-d') }}</td><td>{{ $booking->guest_name ?? $booking->user?->name ?? 'Guest' }}</td><td>{{ $booking->room?->name ?? '—' }}</td><td>₱{{ number_format($booking->total_amount) }}</td></tr>@empty<tr><td colspan="4" class="no-bookings">No transactions yet.</td></tr>@endforelse</tbody></table></div></article></section>
<section class="panel latest-transactions"><div class="panel-heading"><div><p class="admin-kicker">RECENT ACTIVITY</p><h2>Latest transactions</h2></div></div><div class="transaction-grid">@forelse($latestBookings as $booking)<div><small>{{ $booking->created_at->format('Y-m-d H:i') }}</small><b>{{ $booking->guest_name ?? $booking->user?->name ?? 'Guest' }}</b><span>{{ $booking->room?->name ?? 'Room' }}</span><strong>₱{{ number_format($booking->total_amount) }}</strong></div>@empty<p class="empty-copy">Transactions will appear after bookings are placed.</p>@endforelse</div></section>
</main></div></body></html>
