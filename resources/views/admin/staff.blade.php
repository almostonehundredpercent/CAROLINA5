<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Staff access · Carolina</title><link rel="stylesheet" href="{{ asset('css/admin.css') }}"><style>.staff-role{display:flex;gap:8px;align-items:center}.staff-role select{border:1px solid var(--line);border-radius:6px;padding:8px;background:#fff;font:inherit}.staff-role button{border:0;border-radius:6px;padding:8px 10px;background:var(--orange);color:#fff;font-weight:700;cursor:pointer}.role-help{color:var(--muted);font-size:12px;margin:8px 0 18px}</style></head>
<body><div class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="{{ route('home') }}"><span>c</span><b>Carolina</b><small>TRANSIENT & AIRBNB</small></a><nav class="admin-nav"><a href="{{ route('admin.dashboard') }}">Dashboard</a><a href="{{ route('admin.rooms') }}">Rooms</a><a href="{{ route('admin.bookings') }}">Bookings</a><a href="{{ route('admin.walk-ins.create') }}">Walk-ins</a><a href="{{ route('admin.reports') }}">Reports</a><a class="active" href="{{ route('admin.staff') }}">Staff access</a></nav><form method="POST" action="{{ route('logout') }}" class="admin-logout">@csrf<button class="logout-button" type="submit">Log out</button></form></aside>
<main class="admin-main"><header class="admin-topbar"><div><p class="admin-kicker">ACCESS CONTROL</p><h1>Staff roles</h1><p class="admin-subtitle">Give each team member only the tools they need.</p></div></header>@if(session('success'))<div class="admin-flash">{{ session('success') }}</div>@endif @if($errors->any())<div class="admin-flash" style="background:#fbe3e0;color:#a84336">{{ $errors->first() }}</div>@endif
<section class="panel"><p class="role-help"><b>Administrator</b> manages everything. <b>Front desk</b> manages bookings and reviews. <b>Housekeeping</b> manages room blocks. <b>Viewer</b> can inspect the dashboard only.</p><div class="admin-table-wrap"><table><thead><tr><th>Staff member</th><th>Email</th><th>Role</th><th>Save</th></tr></thead><tbody>@forelse($staff as $member)<tr><td><b>{{ $member->name }}</b></td><td>{{ $member->email }}</td><td><form id="staff-{{ $member->id }}" class="staff-role" method="POST" action="{{ route('admin.staff.role', $member) }}">@csrf @method('PATCH')<select name="staff_role">@foreach(['admin' => 'Administrator', 'front_desk' => 'Front desk', 'housekeeping' => 'Housekeeping', 'viewer' => 'Viewer', 'guest' => 'Guest'] as $value => $label)<option value="{{ $value }}" @selected(($member->is_admin ? 'admin' : $member->staff_role) === $value)>{{ $label }}</option>@endforeach</select></form></td><td><button class="staff-role" type="submit" form="staff-{{ $member->id }}">Save</button></td></tr>@empty<tr><td colspan="4" class="no-bookings">No staff accounts are configured yet.</td></tr>@endforelse</tbody></table></div></section></main></div></body></html>
